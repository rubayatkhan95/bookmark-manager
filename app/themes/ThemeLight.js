import { DefaultTheme, DarkTheme } from '@react-navigation/native';
import { ThemeLightColors } from './ThemeColors';

const ThemeLight = {
  ...DefaultTheme,
  colors: ThemeLightColors.colors
};

export default ThemeLight;
